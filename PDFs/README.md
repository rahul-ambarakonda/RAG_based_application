# PDFs Directory

Place your product catalog and brochure PDFs here (e.g. electrical component brochures, product manuals).

The application will search and parse all `.pdf` files in this directory when you click **Start Indexing** in the UI.

*Note: Large PDFs (like catalogs) are ignored by Git in `.gitignore` to avoid repository bloat and GitHub file limits.*
